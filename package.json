{
  "name": "vue-hotel-roomlist",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "json": "json-server --watch db.json --port 8888",
    "start": "concurrently \"vite\" \"json-server --watch db.json --port 8888\""
  },
  "dependencies": {
    "axios": "^1.4.0",
    "concurrently": "^8.2.0",
    "json-server": "^0.17.3",
    "lodash.debounce": "^4.0.8",
    "vue": "^3.2.47",
    "vue-router": "^4.2.2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.0.0",
    "vite": "^4.1.4"
  }
}
